#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../clean-css/bin/cleancss" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../clean-css/bin/cleancss" "$@"
  ret=$?
fi
exit $ret
